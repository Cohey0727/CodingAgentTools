# Every API key lives here (configs.jsonc only references them)
/.env
/.env.bak

# providers/ is not part of the repo; a checkout may still hold key files there
providers/

# Personal data the freelance skills fill their templates with
skills/freelance-invoice/personal-config.json
skills/freelance-work-record/personal-config.json

# Byte-compiled bin/models.py
bin/__pycache__/

# Claude Code local state (per-developer, not shared)
.claude/settings.local.json
.claude/.credentials.json
