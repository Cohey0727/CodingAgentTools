# グローバル作業ルール

Claude Code は `~/.claude/CLAUDE.md`、pi は `~/.pi/agent/AGENTS.md`、Codex は `~/.codex/AGENTS.md` として、このファイルの実体を読む。編集はこのファイルだけで行う。

## コミュニケーション

- ユーザーやその入力を持ち上げる前置きを書かない。「その通りです」「良い指摘です」「鋭い指摘です」「さすが」「ナイスキャッチ」、"Great question" "You're absolutely right" "Good catch" "Excellent point" と、同種の表現を全言語で禁止。
- 別の褒め言葉に言い換えるのではなく、丸ごと削って本文から始める。
- 情報を伴う同意は可（「はい、X です。理由は Y」）。禁止なのは中身のない追従だけ。同意・不同意は理由の記述で示す。

**Why:** ユーザーがこの種の表現を不快に感じており、全セッション・全レポで恒久的に排除したい（2026-06-25 指摘）。

## Git

- **commit と push は 1 つの操作**。commit したら必ず同じタスク内で push する（新規ブランチは `git push -u origin <branch>`）。
- 「commit しろ」は常に「commit して push しろ」の意味。
- push できない場合（remote 無し・認証失敗・保護ブランチ・force 必要）は黙って local に残さず、理由を即報告して指示を仰ぐ。
- コミットメッセージは `<type>: <説明>`。type は feat / fix / refactor / docs / test / chore / perf / ci。
- PR は最新コミットだけでなく `git diff <base>...HEAD` で全変更を確認してから書き、テスト計画を含める。

## コーディング

- **ミューテーション禁止**。オブジェクトを書き換えず、新しい値を返す（`return { ...user, name }`）。
- ファイルは小さく多く。200〜400 行が標準、800 行上限。機能・ドメイン単位で分割し、型別に並べない。
- 関数は 50 行以内、ネストは 4 段まで。
- エラーを握り潰さない。catch したら文脈をログに残し、利用者に意味の分かるメッセージで再送出する。
- 外部入力は必ずスキーマで検証する。
- ハードコードした値・`console.log` を残さない。
- 秘密情報をコードに書かない。環境変数から読み、未設定なら起動時に落とす。
- 新機能の土台は、ゼロから書く前に実績あるスケルトン・既存実装を探して合わせる。

## コメント

コメントは「コードだけでは表現できない制約・理由」だけを書く。以下は全面禁止:

1. **Issue / PR 番号・チケット参照**（`#1234`, `Issue #`, `PR #`）。来歴は git log と Issue が既に持っている。
2. **移行・変更の来歴語り**（「元は X だった」「Y から移行した」「legacy Z を置き換えた」）。今のコードが何であるかだけが重要。
3. **規約文書への参照・レビュー弁明**（「規約 §3 に従う」「レビュー指摘に対応した」）。レビュアー向けの発話で、読み手には無意味。書くなら PR の説明に書く。

書いてよいのは外部制約と非自明な理由だけ:

```typescript
// Yahoo API は 1 リクエスト 200 銘柄まで
const CHUNK_SIZE = 200

// WAL モードでは INSERT で mtime が変わらないため手動 invalidate が必要
invalidateCompanyMap()
```

判定基準: **1 年後の読み手の理解を助けるか、それとも今回のレビュアーへの説明か**。後者なら削除。

## セキュリティ

コミット前に確認する:

- ハードコードされた秘密情報（API キー・パスワード・トークン）が無い
- 入力を検証し、SQL はパラメータ化、HTML はサニタイズしている
- 認証・認可、CSRF、レート制限が効いている
- エラーメッセージが内部情報を漏らしていない

秘密情報の露出を見つけたら即停止し、修正してから鍵をローテートし、同種の箇所を全体で洗う。

## テスト

- カバレッジ 80% 以上。unit / integration / E2E をすべて用意する。
- TDD: テストを先に書いて失敗させ（RED）、最小実装で通し（GREEN）、リファクタする（IMPROVE）。
- テストが落ちたら実装を直す。テスト自体が誤っている場合を除き、テストを緩めない。

## 運用

- `--dangerously-skip-permissions` は使わない。許可は `~/.claude/settings.json` の `allowedTools` で管理する。
- 広い探索・調査はサブエージェントに投げ、メインコンテキストを汚さない。
- 毎回強制したい手続きは、プロンプトに書くより hook にする方が確実でトークンコストがゼロ。
