---
description: repeat one prompt each turn until it is done, a stop condition is met or the budget runs out
---

The loop plugin rewrites this message with the task and the rules of the loop.
Seeing this text means the plugin did not load, so the loop is not running —
check `~/.config/opencode/plugin/loop.js` and restart opencode. Treat the rest
of this message as a single ordinary request.

Task:
$ARGUMENTS
