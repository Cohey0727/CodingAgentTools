---
description: keep working on one objective across turns until it is met, blocked or out of budget
---

The goal plugin rewrites this message with the session's objective and the
continuation contract. Seeing this text means the plugin did not load, so the
loop is not running — check `~/.config/opencode/plugin/goal.js` and restart
opencode. Treat the rest of this message as a single ordinary request.

Objective:
$ARGUMENTS

Work in small steps you can verify: run the project's own checks, re-read what
you changed, and look at real output before claiming a result. Stop when the
objective is met and say what proves it, or when no defensible path remains and
say exactly what would unblock it.
